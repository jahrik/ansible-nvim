[project]
name = "ansible-nvim"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "molecule",
    "molecule-plugins[docker]",
    "ansible-core",
    "docker",
    "ansible-lint",
]
