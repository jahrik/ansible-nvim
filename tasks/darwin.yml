---
- name: Install requirements (macOS)
  become: false
  community.general.homebrew:
    name:
      - fd
      - fontconfig
      - ripgrep
      - tree-sitter-cli
      - unzip

- name: Install neovim (macOS)
  become: false
  community.general.homebrew:
    name: neovim
