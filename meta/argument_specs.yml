---
argument_specs:
  main:
    short_description: Install and configure Neovim
    options:
      install:
        type: bool
        default: true
        description: Set to false to uninstall Neovim and remove its config
